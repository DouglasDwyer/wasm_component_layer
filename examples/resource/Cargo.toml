cargo-features = ["per-package-target"]

[package]
name = "resource_component"
version = "0.1.0"
edition = "2021"

forced-target = "wasm32-unknown-unknown"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.27.0"